import { Router } from "express";

const networksRouter = Router()

//networksRouter.all("/", authenticateToken);
// networksRouter.get("/", getNetworks);
// networksRouter.get("/:id", getEspeficNetwork);
// networksRouter.post("/", createNetwork);
// networksRouter.put("/", updateNetwork);

export default networksRouter;